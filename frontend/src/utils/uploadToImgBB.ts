/**
 * Redirected to Company Cloud Storage
 * All uploads are stored strictly on internal company servers without 3rd-party websites.
 */
import { uploadFile } from './uploadFile';

export const uploadToImgBB = uploadFile;
export default uploadFile;
